#include<bits/stdc++.h>
#include<stdlib.h>
using namespace std;
#define ll long long int
typedef vector<int> vi;
typedef pair<ll,ll> ii;
typedef vector<int,int> vii;
int main()
{
  int t;
  cin>>t;
  for(int t_i=0;t_i<t;t_i++){
    ii a,b;
    ll mul=1;
    cin>>a.first>>a.second>>b.first>>b.second;
    mul=ceil(a.second*1.0/b.second);
    while(1){
    if(
    }
    }
  return 0;
}
